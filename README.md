# AI-Tool-RAG-Weaviate-Pinecone 🚀

Полноценный Python-проект с GUI для работы с RAG (Retrieval-Augmented Generation) системой и векторными базами данных **Weaviate** и **Pinecone**.

## 📋 Описание проекта

Этот проект предоставляет унифицированный интерфейс для работы с различными векторными базами данных в контексте RAG-системы. Вы можете:

- 🖥️ **Использовать удобный GUI** на PyQt6 для работы с системой
- 🧠 Генерировать эмбеддинги с помощью OpenAI's `text-embedding-3-large`
- 💾 Хранить и индексировать документы в векторных хранилищах
- 🔍 Выполнять семантический поиск по документам
- 📊 Сравнивать результаты поиска между разными хранилищами
- 🔄 Легко переключаться между векторными базами данных

## ✨ Новые возможности

### 🖥️ GUI Приложение (PyQt6)
- **3 вкладки** для удобной работы:
  - 📄 **Документы**: добавление текстов и загрузка файлов
  - 🔍 **Поиск**: семантический поиск с настройками
  - 📊 **Сравнение**: сравнение результатов Pinecone и Weaviate
- Асинхронная обработка (не зависает)
- Прогресс бары и статус бар
- Понятные сообщения об ошибках

### 📚 Примеры использования
- `examples/demo_usage.py` - полная демонстрация
- `examples/simple_example.py` - простой пример для начинающих
- `USAGE_GUIDE.md` - подробное руководство

## 🏗️ Структура проекта

```
AI-Tool-RAG-Weaviate-Pinecone/
├── gui_app.py                   # GUI приложение (PyQt6)
├── run_gui.bat                  # Быстрый запуск GUI
├── README.md                    # Документация
├── USAGE_GUIDE.md              # Руководство по использованию
├── requirements.txt             # Зависимости Python
├── config/
│   └── settings.py             # Конфигурация
├── embeddings/
│   └── embedder.py             # Генерация эмбеддингов
├── stores/
│   ├── weaviate_store.py       # Интеграция с Weaviate
│   ├── pinecone_store.py       # Интеграция с Pinecone
│   └── relevance_store.py      # Интеграция с Relevance AI
├── rag/
│   └── retriever.py            # Унифицированный RAG retriever
├── examples/
│   ├── demo_usage.py           # Полная демонстрация
│   └── simple_example.py       # Простой пример
├── scripts/
│   └── check_setup.py          # Проверка настройки
└── utils/
    ├── logger.py               # Логирование (loguru)
    └── chunker.py              # Разбиение текста
```

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/Jeff555max/AI-Tool-RAG-Weaviate-Pinecone.git
cd AI-Tool-RAG-Weaviate-Pinecone
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка .env файла

Создайте файл `.env` в корневой директории:

```env
# OpenAI API Key (обязательно)
OPENAI_API_KEY=your_openai_api_key_here

# Pinecone Configuration (обязательно)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=us-east-1-aws
PINECONE_INDEX_NAME=rag-demo-index

# Weaviate Configuration (обязательно)
WEAVIATE_URL=your_weaviate_cloud_url
WEAVIATE_API_KEY=your_weaviate_api_key_here

# Embedding Configuration
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIMENSION=3072
```

### Получение API ключей

#### OpenAI
1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в API Keys
3. Создайте новый ключ

#### Pinecone
1. Зарегистрируйтесь на [pinecone.io](https://app.pinecone.io/)
2. Создайте проект
3. Скопируйте API ключ

#### Weaviate Cloud
1. Зарегистрируйтесь на [console.weaviate.cloud](https://console.weaviate.cloud)
2. Создайте кластер (Free Sandbox)
3. Скопируйте Cluster URL и API Key

## 🚀 Запуск

### Запуск GUI приложения (рекомендуется)

**Способ 1: Двойной клик**
```
Дважды кликните на run_gui.bat
```

**Способ 2: Командная строка**
```bash
python gui_app.py
```

### Запуск примеров

```bash
# Полная демонстрация
python examples/demo_usage.py

# Простой пример
python examples/simple_example.py

# Проверка настройки
python scripts/check_setup.py
```

## 💡 Использование GUI

### Вкладка "Документы"
1. Введите текст или загрузите файл
2. Выберите векторную БД (Pinecone/Weaviate)
3. Нажмите "Добавить документ"

### Вкладка "Поиск"
1. Введите ваш вопрос
2. Выберите БД и количество результатов
3. Нажмите "Поиск"
4. Изучите результаты с оценкой релевантности

### Вкладка "Сравнение"
1. Введите запрос
2. Нажмите "Сравнить"
3. Увидите результаты из обеих БД

## 📝 Использование в коде

### Базовый пример

```python
from embeddings.embedder import Embedder
from rag.retriever import Retriever

# Инициализация
embedder = Embedder()
retriever = Retriever(embedder)

# Добавление документов
documents = [
    "Python - язык программирования",
    "Machine Learning - раздел AI"
]

retriever.add_documents(
    texts=documents,
    store_type="pinecone"
)

# Поиск
results = retriever.retrieve(
    query="Что такое Python?",
    store_type="pinecone",
    top_k=3
)

# Вывод результатов
for result in results:
    print(f"Score: {result['score']:.4f}")
    print(f"Text: {result['text']}\n")
```

### Сравнение векторных БД

```python
# Сравнить результаты в разных БД
retriever.compare_stores(
    query="Explain machine learning",
    top_k=3
)
```

### Работа с большими текстами

```python
from utils.chunker import TextChunker

chunker = TextChunker(chunk_size=512, chunk_overlap=50)

# Разбить длинный текст
chunks = chunker.chunk_text(long_text)

# Добавить чанки в БД
retriever.add_documents(texts=chunks, store_type="weaviate")
```

## 🔍 Особенности

### Качество кода
- ✅ PEP8 совместимость
- ✅ Полная типизация (Python 3.10+)
- ✅ Подробные docstrings
- ✅ Обработка ошибок
- ✅ Логирование с loguru

### Архитектура
- 🏗️ Модульная структура
- 🔄 Единый интерфейс для всех хранилищ
- 🎯 Легкое переключение между БД
- 📦 Переиспользуемые компоненты
- 🖥️ Современный GUI на PyQt6

### Функциональность
- 🧮 Генерация эмбеддингов через OpenAI
- 💾 Поддержка Pinecone и Weaviate
- 🔎 Семантический поиск
- 📊 Сравнение результатов
- ✂️ Чанкинг текста с учётом токенов
- 🖥️ Удобный графический интерфейс

## 📚 Документация

- **README.md** (этот файл) - основная документация
- **USAGE_GUIDE.md** - подробное руководство с примерами
- **examples/** - примеры использования
- **scripts/check_setup.py** - проверка настройки

## 🐛 Troubleshooting

### GUI не запускается
```bash
pip uninstall PyQt6 -y
pip install PyQt6
```

### Ошибка "API key is required"
Проверьте файл `.env` - все ключи должны быть заполнены

### Ошибка подключения к Weaviate
Убедитесь что URL правильный и начинается с `https://`

### Import ошибки
```bash
pip install -r requirements.txt --upgrade
```

## 🔄 Обновление зависимостей

```bash
pip install --upgrade openai pinecone-client weaviate-client PyQt6
```

## 📄 Лицензия

MIT License - используйте свободно в своих проектах

## 🤝 Вклад

Contributions приветствуются! Создавайте issues и pull requests.

---

**Создано с ❤️ для демонстрации RAG систем с векторными базами данных**
